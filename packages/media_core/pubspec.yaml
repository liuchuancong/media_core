name: media_core
description: A cross-platform reusable media player core.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.28.0
  collection: ^1.19.1
  meta: ^1.16.0
  mime: ^2.0.0
  async: ^2.13.0
  clock: ^1.1.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.26.2
  mocktail: ^1.0.4
  fake_async: ^1.3.3
  flutter_lints: ^6.0.0