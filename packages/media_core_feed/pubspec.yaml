name: media_core_feed
description: Vertical feed playback for media_core - one shared player, swipe-attached items, optional next-item preload (TikTok/Douyin style).
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  media_core:
    path: ../media_core

dev_dependencies:
  flutter_lints: ^6.0.0
