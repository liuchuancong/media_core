name: media_core_download
description: Offline media downloads for media_core - a task queue with concurrency limits, resumable transfers and retry policy.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  media_core:
    path: ../media_core

dev_dependencies:
  flutter_lints: ^6.0.0
